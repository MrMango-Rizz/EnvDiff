# Second broken demo: different app, different failures.

# SECRET_KEY holds a real high-entropy secret -> secret warning
SECRET_KEY=django-insecure-9f8h3kq2mz7x1v0b5n6c4jd8slw3rt9y

DEBUG=True

# DB_PORT out of range -> type error (port)
DB_PORT=70000

# CACHE_URL fine
CACHE_URL=redis://localhost:6379/0

# EMAIL_HOST_USER missing entirely -> (optional? no) -> error? it's not marked required -> error by default
# (left out to show a missing non-required key is still an error)

# SENTRY_DSN empty -> optional, so missing/empty is a warning
SENTRY_DSN=

# GH token accidentally pasted -> secret warning via pattern
DEPLOY_TOKEN=ghp_abcdefghijklmnopqrstuvwxyz0123456789
